
# Venus Savings (Day 2: Landing + Auth Only)

This is a small full‑stack demo for your capstone:

- **Backend (server/)**: Express + JWT + bcrypt, in‑memory users.
- **Frontend (client/)**: React + Vite + React Router.
- Features right now:
  - Landing page that explains the idea.
  - Login / Sign‑up page that talks to the backend.

## How to run it

### 1. Backend

```bash
cd server
cp .env.example .env
# open .env and set JWT_SECRET to a long random string if you want

npm install
npm run dev
```

You should see:

```text
Server running on port 5001
```

### 2. Frontend

```bash
cd client
npm install
npm run dev
```

Then open `http://localhost:5173` in your browser and click **get started**.
